---
phase: 01-core-daemon-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.codywatcher/daemon.ts
  - ~/.codywatcher/daemon.test.ts
autonomous: true
requirements:
  - DAEMON-01
  - DAEMON-02
  - DAEMON-03
  - DAEMON-07
  - DAEMON-08
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Daemon accepts POST /notify and stores request in session map"
    - "Daemon creates per-session FIFO in /tmp/codywatcher/sessions/"
    - "Daemon writes decision JSON to FIFO on demand"
    - "GET /status returns JSON array of pending requests"
    - "Stale sessions (>1 hour) are purged automatically"
    - "Missing FIFO write is caught gracefully (ENOENT), daemon does not crash"
    - "Daemon binds to localhost:18765 only"
  artifacts:
    - path: "~/.codywatcher/daemon.ts"
      provides: "Bun HTTP server with /notify, /status routes, session map, FIFO lifecycle"
      exports: ["Server on port 18765"]
    - path: "~/.codywatcher/daemon.test.ts"
      provides: "Unit tests for session CRUD, FIFO creation, missing FIFO, status endpoint"
  key_links:
    - from: "~/.codywatcher/daemon.ts"
      to: "/tmp/codywatcher/sessions/{sessionId}.fifo"
      via: "mkfifo + fs.openSync write"
      pattern: "mkfifo.*sessionId.*fifo"
    - from: "~/.codywatcher/daemon.ts"
      to: "localhost:18765"
      via: "Bun.serve port binding"
      pattern: "port:\\s*18765"
---

<objective>
Create the Bun HTTP daemon that accepts POST /notify, stores permission requests in a session map, manages FIFO lifecycle, exposes GET /status, and handles stale session cleanup.

Purpose: Core server that all hooks and future voice components depend on.
Output: ~/.codywatcher/daemon.ts with tests, verified by npm/bun test.
</objective>

<execution_context>
@~/.cody-claude/get-shit-done/workflows/execute-plan.md
@~/.cody-claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-daemon-hooks/01-CONTEXT.md
@codywatcher-spec.md

<interfaces>
// Session state structure (per D-02, minimal session state)
interface PendingRequest {
  sessionId: string;
  tool: string;
  prompt: string;
  status: "pending" | "approved" | "denied";
  timestamp: number; // Unix epoch milliseconds
}

// GET /status response format (per D-06)
// Returns: PendingRequest[] — array of all requests in session map
// Example: [{ "sessionId": "abc", "tool": "Bash", "prompt": "...", "status": "pending", "timestamp": 1234567890 }]

// POST /notify request body format
// { "sessionId": "uuid-string", "tool": "Bash", "prompt": "Do you want to proceed?" }

// Decision JSON written to FIFO (Claude PermissionRequestHook format)
// Allow: {"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}
// Deny: {"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"deny","message":"reason"}}}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build daemon HTTP server with /notify and /status endpoints</name>
  <read_first>
    - ~/.codywatcher/daemon.ts (create if not exists)
    - codywatcher-spec.md — architecture spec with endpoint definitions, error handling table
    - .planning/phases/01-core-daemon-hooks/01-CONTEXT.md — decisions D-01 (single file), D-02 (minimal session), D-06 (status format)
  </read_first>
  <behavior>
    - POST /notify with {sessionId, tool, prompt} stores request in Map<sessionId, PendingRequest>, returns 200 with status
    - POST /notify with duplicate sessionId updates existing entry, returns 200
    - GET /status returns JSON array of all entries in session map
    - GET /status on empty map returns []
    - Any unmatched route returns 404
    - Server binds to localhost:18765 only (SEC-01, DAEMON-01)
  </behavior>
  <action>
    Create ~/.codywatcher/daemon.ts as a single Bun.serve() server (per D-01). Use `Bun.serve()` with port 18765 (DAEMON-01). Maintain session state as `Map<string, PendingRequest>` (DAEMON-02). Implement POST /notify handler: parse JSON body, validate {sessionId, tool, prompt} fields, upsert into session map, return `{ status: "stored", sessionId }`. Implement GET /status handler: return `JSON.stringify(Array.from(sessionMap.values()))` (D-06). Return 404 for all other routes. Use `import { $ } from "bun"` for any shell commands. Bind to hostname "127.0.0.1" not "0.0.0.0" to satisfy SEC-01.

    Create ~/.codywatcher/daemon.test.ts with Bun.test():
    - Test POST /notify stores a new session (verify map has entry, response status is "stored")
    - Test POST /notify with same sessionId updates entry
    - Test GET /status returns array of stored requests
    - Test GET /status returns [] when map is empty
    - Test unmatched route returns 404

    Run with: `bun test ~/.codywatcher/daemon.test.ts`
  </action>
  <verify>
    <automated>bun test ~/.codywatcher/daemon.test.ts</automated>
    <manual>curl -s -X POST http://127.0.0.1:18765/notify -H "Content-Type: application/json" -d '{"sessionId":"test-001","tool":"Bash","prompt":"test"}' | jq .</manual>
  </verify>
  <acceptance_criteria>
    - ~/.codywatcher/daemon.ts exists and exports a running Bun.serve() server
    - Server listens on port 18765 bound to 127.0.0.1 (SEC-01)
    - POST /notify accepts JSON with sessionId, tool, prompt fields and returns 200 with status "stored"
    - Session map is `Map<string, PendingRequest>` with fields: sessionId, tool, prompt, status, timestamp (DAEMON-02)
    - GET /status returns JSON array matching `PendingRequest[]` format (D-06, DAEMON-07)
    - daemon.test.ts contains at least 5 test cases covering CRUD and empty state
    - All tests pass: `bun test ~/.codywatcher/daemon.test.ts` exits 0
  </acceptance_criteria>
  <done>HTTP server accepts POST /notify, stores sessions, returns status array on GET /status, all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FIFO creation, decision writing, and stale session cleanup</name>
  <read_first>
    - ~/.codywatcher/daemon.ts (created by Task 1)
    - codywatcher-spec.md — FIFO lifecycle, missing FIFO handling, ENOENT error behavior
    - .planning/phases/01-core-daemon-hooks/01-CONTEXT.md — decisions D-03 (per-session FIFO, hook pre-creates), D-04 (1-hour purge), D-09 (30s timeout — hook side)
  </read_first>
  <behavior>
    - On POST /notify for new session: attempt mkfifo at /tmp/codywatcher/sessions/{sessionId}.fifo (D-03, SEC-02)
    - If FIFO already exists (hook pre-created): skip creation, store session normally
    - On POST /shutdown (optional) or internal call: write decision JSON to session's FIFO
    - Decision format: {"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}} for allow, with "deny"+"message" for deny
    - If FIFO does not exist on write (ENOENT): catch error, log warning, do not crash (SEC-03)
    - Stale sessions (>1 hour): purge from session map, log warning (DAEMON-08)
    - Stale purge runs on 15-minute interval via setInterval
  </behavior>
  <action>
    Extend ~/.codywatcher/daemon.ts with FIFO and cleanup logic (still single file, per D-01).

    FIFO creation (DAEMON-03, SEC-02): In POST /notify handler, before storing session, ensure /tmp/codywatcher/sessions/ directory exists (`mkdir -p` via Bun.$). Then attempt `mkfifo /tmp/codywatcher/sessions/${sessionId}.fifo` via Bun.$. Catch "file exists" — hook may have pre-created it (D-07). Store session with status "pending" and Date.now() timestamp.

    Decision writing function `writeDecision(sessionId: string, decision: "allow" | "deny", message?: string)`: Open the session's FIFO for writing using `fs.openSync(fifoPath, "w")`. Write JSON string matching Claude's PermissionRequestHook format (from CONTEXT.md specific ideas: `{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}` for allow, same with "deny" + "message" for deny). Close fd. Catch ENOENT — log `console.warn(`FIFO not found for session ${sessionId}`)`, do not throw (SEC-03).

    Stale cleanup (DAEMON-08): `setInterval` every 15 minutes iterates sessionMap, removes entries where `Date.now() - entry.timestamp > 3600000` (1 hour). Log count of purged sessions.

    Extend daemon.test.ts with:
    - Test FIFO creation creates pipe file in /tmp/codywatcher/sessions/
    - Test duplicate FIFO creation does not throw (pre-created case)
    - Test writeDecision with missing FIFO catches ENOENT, logs warning, does not crash
    - Test stale session purge removes entries older than 1 hour
  </action>
  <verify>
    <automated>bun test ~/.codywatcher/daemon.test.ts --filter "FIFO|cleanup|decision"</automated>
    <manual>ls -la /tmp/codywatcher/sessions/ | head -5</manual>
  </verify>
  <acceptance_criteria>
    - POST /notify creates /tmp/codywatcher/sessions/ directory if not exists
    - POST /notify calls mkfifo for /tmp/codywatcher/sessions/{sessionId}.fifo (DAEMON-03)
    - Existing FIFO (pre-created by hook) does not cause error or overwrite (D-07)
    - writeDecision function writes correct JSON format to FIFO
    - writeDecision catches ENOENT on missing FIFO without crashing (SEC-03)
    - setInterval purges sessions older than 3600000ms (DAEMON-08)
    - daemon.test.ts contains tests for FIFO creation, missing FIFO, stale purge
    - All tests pass: `bun test ~/.codywatcher/daemon.test.ts` exits 0
  </acceptance_criteria>
  <done>FIFO lifecycle works end-to-end, stale cleanup runs on interval, all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost:18765 ← hook.sh | Untrusted JSON body POSTed to /notify — must validate shape |
| /tmp/codywatcher/sessions/ ← daemon | FIFO files created in /tmp with UUID names (SEC-02) |
| FIFO write ← daemon | ENOENT from deleted FIFO must not crash daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | POST /notify | mitigate | Validate JSON body has required fields (sessionId, tool, prompt); reject malformed requests with 400 |
| T-01-02 | Tampering | FIFO files in /tmp | mitigate | Use UUID session IDs from Claude Code (D-12); FIFO names are unpredictable |
| T-01-03 | Information Disclosure | GET /status | accept | localhost-only binding (127.0.0.1), no network exposure (SEC-01) |
| T-01-04 | Denial of Service | Session map | mitigate | Stale session purge every 15min removes entries >1hr old (DAEMON-08) |
| T-01-05 | Elevation of Privilege | daemon.ts process | mitigate | No setuid, no elevated permissions needed — runs as user |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `bun test ~/.codywatcher/daemon.test.ts` passes all tests
- `bun run ~/.codywatcher/daemon.ts` starts and listens on 127.0.0.1:18765
- `curl -s http://127.0.0.1:18765/status` returns `[]`
- `curl -s -X POST http://127.0.0.1:18765/notify -H "Content-Type: application/json" -d '{"sessionId":"test-123","tool":"Bash","prompt":"test prompt"}'` returns `{"status":"stored","sessionId":"test-123"}`
- `ls -p /tmp/codywatcher/sessions/` shows FIFO file (type `p`)
</verification>

<success_criteria>
- daemon.ts is a single Bun HTTP server on localhost:18765 (D-01, SEC-01, DAEMON-01)
- POST /notify stores requests with sessionId, tool, prompt, status, timestamp (D-02, DAEMON-02)
- Per-session FIFO created in /tmp/codywatcher/sessions/ (D-03, SEC-02, DAEMON-03)
- writeDecision writes Claude-compatible JSON to FIFO, handles ENOENT gracefully (SEC-03)
- GET /status returns JSON array of pending requests (D-06, DAEMON-07)
- Stale sessions purged after 1 hour on 15-minute interval (D-04, DAEMON-08)
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/01-core-daemon-hooks/01-01-SUMMARY.md` when done
</output>
